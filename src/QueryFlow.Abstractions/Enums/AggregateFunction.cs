namespace QueryFlow.Abstractions.Enums;

/// <summary>
/// A supported aggregate function that can be computed over a query.
/// </summary>
public enum AggregateFunction
{
    Count,
    Sum,
    Average,
    Max,
    Min
}
